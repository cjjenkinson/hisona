import { defaultTheme } from 'smooth-ui'

import colors from './colors'
import { type, style } from './fonts'
import { metrics } from './metrics'

// https://smooth-ui.smooth-code.com/

const theme = {
  ...defaultTheme,
  primary: colors.primary,
}

export default theme
